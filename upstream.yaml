schema_version: "vyges-upstream-v1"
upstream:
  type: "monorepo"
  source: https://github.com/google-coral/coralnpu
  path: hdl/chisel
  commit: 88befd73c010e0fc7bbb9fa7f50bcdf3e99bf8ee
  extracted_at: "2026-04-01T05:49:31Z"
  note: "SystemVerilog generated from Chisel via Bazel (bazelisk build, repo-pinned Bazel 7.4.1). Emitted with firtool --disable-layers=Verification (set in CoralNPUChiselSubsystem.scala emitter) so the RTL is synthesis-clean — no SVA `bind` verification-layer blocks (sv2v cannot parse `bind`; Verilator/Vivado can). See BUILD emitter targets."
import:
  rtl:
    - path: "rtl"
      map_to: "rtl"
  license:
    - path: "Apache-2.0"
license:
  spdx: Apache-2.0
  file: LICENSE
metadata_locked: true
# Vyges-authored content — excluded from upstream sync (net-new; no upstream
# path collision). vyges-additions/ holds the integration wrapper + its
# generator; rvv_inc/ holds the RVV backend headers the subsystem RTL includes.
vyges_authored:
  exclude_from_sync:
    - "vyges-additions/"
    - "rvv_inc/"
